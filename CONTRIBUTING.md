# 贡献配置

1. 先找同voice/keys资源，能引用就不复制。标签或布局不同只新增model；键码不同新增keys；已有驱动参数都相同就共用voice。
2. 使用稳定ID与revision1。已发布资源不可改写；修订新增版本文件并更新引用。
3. 提供设备名称、PnP、服务和report拓扑、完整Report Map、逐键及语音验证结果。移除MAC、序列号、密钥、个人录音、时间线及完整原始日志。
4. 无明确型号使用sample名称，避免宣称兼容整类品牌。相同指纹不同键表必须说明区分方法，不能声称可自动唯一匹配。
5. 运行校验并生成catalog索引，提交PR。CI检查schema、引用、摘要、布局覆盖与旧修订不被改写。

内容由贡献者原创或有权按本仓库MIT许可提交。不要提交厂商固件或反编译源码。
