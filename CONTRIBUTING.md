# 贡献机型配置

1. 先采集，后提交。需要完整Report Map及摘要、要求使用的PnP/服务/报告字段，说明逐键和语音验证范围；未知字段不填猜测值。
2. 按已有目录命名：有准确型号用厂商/型号；无型号用运营商/键数/样本特征，不宣称兼容整个品牌。
3. 能共享协议、键表或布局则引用；根据实测判断，不能仅凭外观或名称认为键码相同。只改用户偏好不要提交新机型。
4. 更新直接编辑固定文件并递增revision。新增机型使用新的稳定ID；不要复用已删除按钮ID。默认动作和名称写defaults，个人override不提交。
5. 移除MAC、个人绑定、序列号、密钥、私人录音和完整运行日志。保留脱敏的识别证据；不得提交厂商固件或反编译源码。
6. 运行 `python tools/validate.py --write-index`、`python tools/validate.py`、`python -m unittest discover -s tests -v`。PR会校验已变更资源revision递增。
7. 相同指纹指向不同机型时，说明用户如何选择或验证，不能声称自动唯一识别。

内容由贡献者原创或有权按本仓库MIT许可提交。
