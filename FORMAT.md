# 格式2：默认库、个人覆盖与设备配置

## 1. 身份与复用边界

机型 ID 标识兼容的硬件定义，不是蓝牙名字、文件路径或某只设备地址。按钮 ID 标识物理按钮，改名或移动布局不能改 ID，也不能把旧 ID 复用于另一按钮。现有 b01 等 ID 保留，文件名负责可读性。

一份 fingerprint 只指向 model，model 再引用 protocol、keymap、layout、defaults；消除指纹重复引用键表与协议造成的不一致。引用仅使用稳定 ID，整份 catalog 快照确定具体 revision 和 SHA256，不在每层重复写版本。

同一型号可以有多份指纹；同协议不等于同机型。协议共享只涉及已支持的算法/行为。原始键码一致才共享 keymap；几何一致才共享 layout；功能和名称由 defaults 分开保存。当前联通/移动共享协议和布局，键表不可合并。小米同名不同协议也不能合并。

目前没有跨机型的默认动作继承树，避免隐式多级覆盖。每份 defaults 必须完整覆盖本机型按钮，数量很小，明确性优先。

## 2. 识别

广播名称、前缀、company 是扫描线索，不能作为同款认证或保存唯一性约束。用户自建机型不因名称重复被拒绝。

连接后匹配完整 Report Map 长度/SHA256；CRC32C用于校验；并核对指纹要求的 PnP source/vendor/product、服务、Report ID/type。服务和报告是必需子集。句柄、RSSI、电量、固件版本字符串、布局位置不作为型号唯一标识。硬件版本确有协议差异时需新增指纹和对应定义，不能根据名称猜测。

四种结果：唯一匹配、多个机型匹配、资料不足、未知。多个匹配不得按顺序或分数强选；缺字段不算匹配。如果一个候选已经通过而另一候选仍缺字段，也先补读，不能提前选。多份指纹通过但都指向同一 model 不算歧义。

完全相同的可读硬件特征却不同键位时，没有凭空存在的唯一指纹；让用户选择对应款式/做区分键验证，并为这只设备保存选择。不得用私人MAC限制公共机型。只有连接读取失败才重试连接，不将“无法唯一识别”伪装成配对失败。

## 3. Default 与 override

公共 defaults 只包含按钮默认 label/action。动作是可读对象，例如 `{"type":"app","command":"task-view"}`、`{"type":"none"}`，不暴露旧固件数字三元组。底层 usage 仍使用 USB HID 数值；键表中的 usage 是遥控器原始输入，两者不是一回事。

个人 override 使用独立 `schemas/user-override.schema.json`，只存在用户本地，按稳定设备 ID + model ID 隔离。设备 ID 由绑定身份建立，不能使用会变化的槽位序号、广播地址或显示名字。删除后重新添加相同设备是否恢复个人设置由保留的绑定身份决定，不能只看名字套用。

有效设置 = 已采用默认快照 + 本设备字段覆盖。未出现的字段继承；明确 none 禁用；显式设置为当前默认值仍是覆盖。action 整体替换，禁止深合并残留 usage/modifiers。恢复默认删除对应覆盖字段；清空按钮覆盖后删除空对象。

override 不能改协议、指纹、原始键码或语音按钮语义。修改这些属于本地机型派生，需要独立命名空间 `local.*` 和验证，贡献审核后才进入公共库。自定义脚本/应用路径等个人动作不进入公共库；扩充动作类型须先扩展客户端能力与格式，不能下载任意代码执行。

用户别名、布局个性化若以后开放，应作为独立展示偏好，不用别名改变型号指纹。当前 override 仅支持按键名称与动作，未声称支持任意布局覆盖。

## 4. 更新及冲突

下载新库只更新可用目录，不悄悄替换已绑定设备的有效快照。用户采用新默认版本时：保留 override，继承字段使用新默认；校验按钮 ID、语义、驱动能力；生成新有效设置并写入接收器、回读确认后，才提交本地 adopted snapshot 与 revision。

被删除的按钮仍有覆盖、按钮语义变化、驱动不支持都必须作为更新冲突，保留旧快照/覆盖并提示处理；不能静默丢弃。失败保留旧配置，重连先核对接收器生效版本再决定重试，不用本地缓存直接覆盖未知状态。

本地只保留使用中的默认快照和一次事务回退所需快照；不无限缓存全部历史。取消编辑不产生覆盖或设备写入，确认保存才提交。写入采用配置代次校验，另一台电脑已修改则拒绝过期写入。

旧应用只有有效映射，无法反推出用户意图。未来迁移应把每个旧有效值作为显式 override 保存，包括恰好等于默认的值；用户可主动恢复默认。禁止凭比较相等就丢弃个人设置。

## 5. 设备端与容量

接收器需要脱离应用工作，因此保存编译后的有效键表/动作及绑定，而不是依赖应用每次解释。公共库原始资源留在电脑，不把整个机型库及历史塞进板子。

后续固件需区分共享硬件 profile（协议参数+原始键表）与每设备生效配置；相同硬件可复用 profile，不同默认名称不占新硬件槽。只安装已绑定/准备配对所需 profile，未引用 profile 才能回收。现有16槽是固件已实现的容量限制，本次数据结构没有凭空解除该限制。

要支持电脑A设置、电脑B继续编辑且知道覆盖来源，接收器管理接口还需要携带 model ID、默认快照标识、配置代次和覆盖来源；当前固件尚未具备完整能力。缺失时电脑B只能把读到的有效映射按显式覆盖保护，不能猜默认来源。

## 6. 版本、文件和发布

固定可读路径，例如 `models/xiaomi/remote-2-pro-rc003.json`。每个 kind+id 在当前库只出现一次；修改原文件并递增 revision，路径移动不改变身份。历史由Git提交/tag保存，不保留数字版本文件，不写兼容旧格式的加载分支。

format_version=2 是解析契约；catalog_version=0.2.0 是此次未正式稳定前的结构重置；后续证据/默认修正 patch、新机型 minor，不兼容格式主版本递增。driver_api 和 minimum_catalog_api 是能力要求，不由显示版本猜能力。

catalog 是完整快照清单，带每份文件字节长度和SHA256。同步固定 Git commit/tag，不能边下载边跟随 main；检查重复ID、安全相对路径、大小上限、摘要、Schema及所有引用闭包，完成后原子替换目录。摘要只保证内容完整性，仓库来源信任单独由应用固定可信仓库处理。

当前 main 清除格式1结构和不完整RC003指纹；旧提交仍作为审计历史保留，不强推抹除历史。旧发布tag退出发布入口，新的使用v0.2.0。

## 7. 已实现边界

本仓库已经转换全部现有数据，具备严格Schema、目录/摘要/引用/布局验证、识别与覆盖参考实现及回归测试。客户端加载器、同步、个人覆盖持久化、事务协议和容量优化仍需按此契约接入，不得把参考实现说成桌面应用已上线。
